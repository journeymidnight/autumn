Date: 2026-03-17
TaskStatus: completed
Task scope: 实现 F017 (autumn-ps binary) 和 F018 (autumn-client CLI)，补全 Go->Rust 移植差距分析。

Current summary:
- F017: autumn-ps binary 创建完成，支持 --psid/--port/--manager/--data/--advertise 参数，注册到 manager，同步 regions，提供 PartitionKv gRPC 服务。
- F018: autumn-client CLI 创建完成，支持 bootstrap/put/get/del/head/ls/split/info 子命令，通过 manager GetRegions 获取 PS 地址路由请求。
- Proto 更新：GetRegionsResponse 新增 ps_details 字段，返回 PS 地址映射。
- Manager 更新：get_regions 返回 ps_nodes 信息。
- PartitionServer 更新：支持 connect_with_advertise，注册时使用真实地址。
- feature_list.json 新增 F017-F025（完整差距分析），F017/F018 passes:true。

What is already implemented (high confidence):
- Proto + gRPC 服务骨架可用（stream/partition/extent）。
- IO engine: Standard + Blocking 后端可用。
- Manager 核心流控：register/create/info/check_commit/alloc/punch/truncate/split。
- ExtentNode 核心 API：append/read/copy/recovery/re_avali/df/heartbeat/commit。
- StreamClient 写路径与流操作接口。
- PartitionServer KV API + split + flush + table/WAL replay 恢复。
- 独立进程 binary: manager, extent-node, stream-cli, partition-server(autumn-ps), autumn-client。
- etcd 持久化。

Main gaps to full Go->Rust migration:
- F010: 缺少 Batch/StreamPut/Maintenance API。
- F011: 缺少 compaction/GC/value-log 等高级存储行为。
- F012: 未见完整 Rust EC 模块。
- F019: Partition Manager 缺少分配策略/负载均衡。
- F020: 缺少 gRPC 连接池。
- F021: 缺少多磁盘支持。
- F022: 缺少 embedded etcd。
- F023: 缺少 REST gateway。
- F024: 缺少分布式 tracing。
- F025: 缺少 stream benchmark CLI。

Next steps:
1) F010: 补 Partition Batch/StreamPut/Maintenance API。
2) F011: 按 feature 拆分实现 partition layer compaction/GC/value-log。
3) F012: 设计并实现 EC 编解码与恢复闭环。

Handover note:
- autumn-ps binary: autumn-rs/crates/server/src/bin/partition_server.rs
- autumn-client binary: autumn-rs/crates/server/src/bin/autumn_client.rs
- Proto 新增 ps_details 在 GetRegionsResponse 中。
- 所有 binary 在 autumn-rs/crates/server/src/bin/ 下。
