Date: 2026-04-05
TaskStatus: completed
Task scope: F019 — Partition Manager complete implementation

Current summary:
  F019 fully implemented. Key additions:

  1. Proto (autumn.proto):
     - Added HeartbeatPsRequest / HeartbeatPsResponse messages
     - Added HeartbeatPs RPC to PartitionManagerService

  2. Manager (crates/manager/src/lib.rs):
     - ps_last_heartbeat: Arc<Mutex<HashMap<u64, Instant>>> field on AutumnManager
     - register_ps: records initial heartbeat timestamp
     - heartbeat_ps RPC: updates timestamp for known PS
     - ps_liveness_check_loop (10s interval): evicts PSes with no heartbeat in 30s,
       calls rebalance_regions, mirrors to etcd
     - rebalance_regions: upgraded to least-loaded allocation (min partition count);
       always refreshes rg from PartitionMeta (critical for post-split range update)

  3. Partition Server (crates/partition-server/src/lib.rs):
     - heartbeat_loop: sends HeartbeatPs to manager every 5s
     - region_sync_loop: polls GetRegions every 5s to pick up reassignments
     - Both loops spawned in connect_with_advertise

  4. Tests (manager/src/lib.rs unit tests):
     - f019_least_loaded_allocation: 2 PSes, 4 partitions → each gets 2
     - f019_ps_eviction_reassigns_regions: remove dead PS → all regions move to survivor
     - f019_heartbeat_updates_timestamp: heartbeat_ps records recent timestamp

  5. Documentation: crates/manager/CLAUDE.md updated

  All tests pass: 5 manager unit tests, 13 manager integration tests, full workspace.

Main gaps:
  F021 (multi-disk support) - not implemented

Next steps:
  F021 — Multi-disk support (multiple data dirs per extent node)
