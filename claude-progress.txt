Date: 2026-04-20
TaskStatus: completed
Task scope: F099-M — multi-thread ExtentNode (per-shard runtime + port)

Branch: perf-r1-partition-scale-out
Worktree: /data/dongmao_dev/autumn-perf-r1
Main worktree /data/dongmao_dev/autumn (branch compio) MUST NOT be touched.

This session completed F099-M:
  - Popped stash@{0} (17-file WIP from prior subagent run).
  - Fixed shards=4 cluster bring-up bug: cluster.sh was not passing
    `--shards` or `--shard-ports` to the extent-node or register-node.
    Without shard_ports on the node entry, the manager routed ALL
    extents to shard 0's port (9101/...), which rejected any extent
    where `extent_id % 4 != 0` with FailedPrecondition, surfacing as
    PS "rowStream commit_length failed ... available nodes 0 less
    than required 1 for extent 10".
  - Fix: cluster.sh now reads $AUTUMN_EXTENT_SHARDS (+stride) and
    (1) spawns the extent-node with --shards/--shard-stride, and
    (2) passes --shard-ports to `autumn-client register-node`.
  - Added 4 planned F099-M integration tests in
    crates/stream/tests/f099m_shards.rs:
      * f099m_shards_serve_disjoint_extents
      * f099m_register_node_reports_shard_ports
      * f099m_client_routes_by_extent_id_modulo
      * f099m_recovery_per_shard
    All pass. 40/40 autumn-stream lib + 84/84 autumn-partition-server
    lib + 4/4 new F099-M tests all green.
  - Perf validated:
      * N=4 × d=8 (3-rep): write 62-70 k ops/s (median 67 k).
        Above 57 k pre-F099-M baseline (+17 %) but below 110 k gate.
      * Fanout p50 1.6 ms (was 129.6 ms) — 80x reduction.
        Architectural fix is working.
      * Extent-node CPU ~20 % per process (no longer saturated).
      * PS P-log threads now at 100 % CPU — the new binding, as
        predicted in F099-K-diagnosis.
      * N=1 × d=1 regression check: 42.4 k write / 50.5 k read
        (≥ 41 k baseline — no regression at shards=1 legacy path).
  - Verdict: DONE_WITH_CONCERNS — architecture works, throughput
    partial. The extent-node is no longer the bottleneck. The 67→110 k
    gap is a PS-side (P-log) follow-up (not in F099-M scope).

Key files touched (from stash + this session):
  NEW:
    - autumn-rs/crates/stream/tests/f099m_shards.rs
  MODIFIED by stash:
    - autumn-rs/crates/stream/src/extent_node.rs (+214)
    - autumn-rs/crates/stream/src/client.rs (+18)
    - autumn-rs/crates/stream/src/conn_pool.rs (+23: shard_addr_for_extent)
    - autumn-rs/crates/stream/src/lib.rs (export shard_addr_for_extent)
    - autumn-rs/crates/server/src/bin/extent_node.rs (+182)
    - autumn-rs/crates/server/src/bin/autumn_client.rs (+18: --shard-ports)
    - autumn-rs/crates/rpc/src/manager_rpc.rs (+10: shard_ports field)
    - autumn-rs/crates/manager/src/lib.rs (+46: shard_ports_for_addr)
    - autumn-rs/crates/manager/src/rpc_handlers.rs (stores shard_ports)
    - autumn-rs/crates/manager/src/recovery.rs (shard-routed recovery)
    - autumn-rs/crates/manager/tests/*.rs (shard_ports: vec![] fixtures)
    - autumn-rs/crates/stream/tests/stream_sqcq.rs (shard_ports: vec![])
  MODIFIED this session:
    - autumn-rs/cluster.sh (+30: forward shards / shard_ports)
    - feature_list.md (add F099-M entry)
    - claude-progress.txt (this file)

Next session can pick up from this baseline:
  - Optional follow-up (F099-N?): close N=4 d=8 67 k → 110 k gap.
    Bottleneck profile: PS 4× part-N threads at 100 % user CPU.
    Candidates: group-commit batch accumulation tuning, bpftrace
    attribution of the P-log hot loop (decode+dispatch vs apply),
    lock-free memtable sentinel path.
  - F099-M is architecturally complete; no further changes needed
    to the extent-node side to unblock higher throughput.
