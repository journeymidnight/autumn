Date: 2026-03-27
TaskStatus: completed
Task scope: F029 – Compaction engine with merge iterator

Current summary:
  Implemented size-tiered compaction engine in Rust partition server.
  - TableMeta struct: tracks extent_id, offset, estimated_size, last_seq per SSTable.
  - DefaultPickupPolicy: headRatio=0.3 (head extent truncation), compactRatio=0.5 (size-tiered), n=5 max tables.
  - do_compact: reads selected tables from sst_cache, merges entries (newest-seq wins via BTreeMap order),
    drops deleted/expired in major mode, splits output into ≤2×MAX_SKIP_LIST chunks, appends to rowStream,
    atomically updates kv index + tables list, persists to metaStream.
  - background_compact_loop: random 10-20s timer for minor compaction, bounded channel for major compaction.
  - trigger_major_compact(part_id): public API to trigger major compaction.
  - All 8 integration tests pass, including new f029_compaction_merges_small_tables test.

  Also fixed: extent_node.rs append path - removed per-append RPC to manager (now only RPC on version mismatch),
  cached manager gRPC channel with tokio::sync::OnceCell.

Main gaps:
  No discard tracking (F033 GC) - compaction does not accumulate discard maps for value log extents.
  logStream integration deferred to F031.

Next steps:
  F031 – logStream (value log) integration
  F032 – SSTable bloom filter, prefix compression, block cache
  F033 – GC with discard tracking (depends on F029 discard maps, F031 value log)
