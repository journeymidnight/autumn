Date: 2026-04-01
TaskStatus: completed
Task scope: F010 – Partition API parity with Go legacy endpoints

Current summary:
  Implemented F010: Maintenance RPC + CLI parity with Go autumn-client.

  Changes in autumn-rs/crates/proto/proto/autumn.proto:
  - Added CompactOp, AutoGcOp, ForceGcOp, MaintenanceRequest, MaintenanceResponse messages.
  - Added Maintenance RPC to PartitionKv service.

  Changes in autumn-rs/crates/partition-server/src/lib.rs:
  - trigger_major_compact(part_id) -> Result<(), &'static str>  (was void)
  - trigger_gc(part_id) -> Result<(), &'static str>             (was void)
  - trigger_force_gc(part_id, extent_ids) -> Result<(), &'static str>  (new)
  - maintenance() gRPC handler: dispatches oneof op to trigger_* methods.

  Changes in autumn-rs/crates/server/src/bin/autumn_client.rs:
  - compact <PARTID>: trigger major compaction via Maintenance RPC
  - gc <PARTID>: trigger auto GC via Maintenance RPC
  - forcegc <PARTID> <EXTID>...: force GC specific extents
  - format --listen --advertise <DIR>...: format disk dirs, RegisterNode, write node_id/disk_id
  - wbench [--threads] [--duration] [--size]: concurrent write benchmark + write_result.json
  - rbench [--threads] [--duration] <RESULT_FILE>: concurrent read benchmark
  - bootstrap: add --presplit N:hexstring to create N pre-split partitions (hex_split_ranges)

  Dependencies added to autumn-rs/crates/server/Cargo.toml:
  - serde, serde_json (bench JSON output)
  - rand (not actually used yet; reserved for future random key gen)
  - uuid (format disk UUID marker)

  Documentation updated:
  - autumn-rs/crates/proto/CLAUDE.md: Maintenance RPC entry in PartitionKv table
  - autumn-rs/crates/partition-server/CLAUDE.md: trigger_force_gc, trigger_major_compact notes
  - autumn-rs/crates/server/CLAUDE.md: full command table updated
  - autumn-rs/README.md: F010/F018 test sections with new CLI examples

  Tests: cargo test --workspace passes (12 unit + 13 integration).

Main gaps:
  Batch API intentionally skipped (was never implemented in Go either - stub only).

Next steps:
  F035 – Extent node WAL for small-write durability
  F020 – gRPC connection pool with health check
