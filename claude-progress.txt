Date: 2026-04-20
TaskStatus: not_completed
Task scope: F099-K committed; N=4 scaling not yet verified; F099-A/B deferred to next round

Branch: perf-r1-partition-scale-out
Worktree: /data/dongmao_dev/autumn-perf-r1
Main worktree /data/dongmao_dev/autumn (branch compio) MUST NOT be touched.

Current summary:
  R4 + F099 (A-K) committed this session. 16 commits on branch.
  Architecture is Seastar-style thread-per-partition:
    - Each partition binds its own TcpListener (base_port + ord)
    - Each partition's P-log runtime hosts: accept task + N ps-conn tasks +
      merged_partition_loop + compact + gc + flush_dispatcher
    - P-bulk per partition still separate (128MB SST upload isolation)
    - No central accept thread, no central Dispatcher, no PartitionRouter
    - Client resolves part_id → port via manager's part_addrs in GetRegions

Perf state (last measured):
  Pre-R4 baseline:     51.7k write / 80k read     (F099-D-era earlier runs)
  Post F099-D:         57.6k write / 71.3k read   (peak of R4+F099 cycle)
  Post F099-J:         42.8k write / 38.8k read   (single-thread saturation)
  Post F099-K N=1:     41.5k write / 45.4k read   (same as F099-J — 1 partition still limited)
  Post F099-K N=4:     41.3k write / 54.8k read   (NOT scaling — diagnosis required)

Commits this session (newest first):
  d5228d2 perf(F099-K): per-partition listener + benchmark routing
  9c9f65e perf(F099-J): collapse PS worker threads into P-log
  10a4900 docs(F099-H): kernel RTT decomposition
  d5278ab perf(F099-D): merge partition_thread_main + write loop
  c66a501 perf(F099-C): Memtable SkipMap -> RwLock<BTreeMap>
  a0a5548 docs(F099-A): R4 ceiling flame graph diagnosis
  37a0f6a perf(F099-B): StreamClient parallel 3-replica fanout
  644f2f0 perf(R4): autumn-client perf-check --pipeline-depth
  c25f436 perf(R4): ps_bench — PS RPC pipeline-depth matrix
  c06dee0 perf(R4): PS P-log + P-bulk SQ/CQ (step 4.4)
  199c345 perf(R4): StreamClient per-stream SQ/CQ worker (step 4.3)
  1e7e456 perf(R4): ExtentNode true SQ/CQ (step 4.2 v3)
  b1a92f7 perf(R4): ExtentNode inline FuturesUnordered (step 4.2 redesign)
  3261702 perf(R4): ExtentNode per-extent SQ/CQ worker (step 4.2)
  b5d53ca perf(R4): RpcClient SQ writer task (step 4.1)
  fc5d0e0 perf(R4): spec — SQ/CQ pipeline architecture (F098)

Key findings from F099-H (kernel flame graph):
  - Write ceiling at N=1 × 256 × d=1 is CPU-bound on kernel TCP stack,
    not user-space CPU.
  - 91% of tcp_sendmsg calls carry 32-63B (tiny PutResp frames), at 22µs
    each, = 0.8 cores burning on small-frame TCP delivery.
  - Reads scale naturally (187k) because reply is one big frame.
  - P-log at pre-F099-J baseline was only ~57% user CPU (the other 43%
    was idle waiting in io_uring_enter for remote CQEs — pprof couldn't
    see this).

Test state:
  autumn-partition-server: 79/79 lib tests green (77 pre-F099-K + 2 new)
  autumn-stream: 40 lib + 21 integration green
  autumn-rpc: 17 green
  Full workspace builds clean (excluding autumn-fuse pre-existing env issue).

Documentation:
  - /data/dongmao_dev/autumn-perf-r1/docs/superpowers/specs/
      2026-04-18-perf-r4-sqcq-pipeline-design.md  (R4 spec)
      2026-04-20-perf-r4-ceiling-diagnosis.md     (F099-A)
      2026-04-20-perf-f099-h-kernel-rtt.md        (F099-H)
  - autumn-rs/CLAUDE.md: updated thread model
  - crates/partition-server/CLAUDE.md: updated write-path
  - crates/stream/CLAUDE.md: updated SQ/CQ, parallel fanout

Main gaps (carry into next session):
  1. F099-K N=4 does NOT show expected 4× scaling.
     Likely causes (need diagnosis):
     a. Benchmark client distribution — verify all 256 threads actually
        connect to ports 9201-9204 in balanced ratio. `ss -t state
        established` during the bench run.
     b. ExtentNode bottleneck — 3 extent-nodes shared by all 4 partitions;
        check per-node CPU + io_uring_enter stats. If saturated, each
        partition is limited by shared extent node capacity, not local
        P-log.
     c. StreamClient per-stream serialization — post-F099-K, each partition
        has its own StreamClient; verify each has a separate ConnPool.
     d. autumn-client benchmark — 256 threads in one client process might
        have shared-channel contention (aggregation paths); verify with
        `top -H -p <client-pid>`.
  2. `autumn-client info` command still shows ps=127.0.0.1:9201 for all
     partitions (display bug — uses ps_details not part_addrs). Fix is
     ~5 lines in crates/server/src/bin/autumn_client.rs info handler.
     Display-only; doesn't affect routing.
  3. F099-I (per-conn reply batching) not implemented. Only useful if
     --pipeline-depth > 1 is exercised.

Next steps (explicit priority order from user):

  A. Diagnose F099-K N=4 scaling gap.
     - Verify client connection distribution across ports (ss -t during run)
     - Verify each partition's P-log CPU % (top -H)
     - Verify extent-node CPU % (not bottleneck)
     - Expected: should see ~4× throughput with each partition ~25% CPU
     - If bottleneck is elsewhere (e.g., client-side thread count / shared
       state), adjust accordingly.

  B. Implement F099-I (per-conn reply batching).
     - Scope: handle_ps_connection inner loop in crates/partition-server/src/lib.rs
     - Change: decode ALL frames from one TCP read, fire to partition
       concurrently, collect oneshot responses into Vec<Bytes>, write_vectored_all
     - Net effect: if client uses --pipeline-depth > 1, server coalesces N
       replies into one tcp_sendmsg (saves 0.8 cores of small-frame TCP CPU)
     - Target workload: 32 threads × d=8 with F099-K N=4 = 8 conns per partition
       each with 8 in-flight. Server should comfortably exceed 100k write.

  C. (If A+B clear gate) Tier verdict + feature_list + merge-decision gate.

Do NOT touch main worktree at /data/dongmao_dev/autumn (branch compio).

The work has been commit-clean. Next session starts from HEAD = d5228d2.
