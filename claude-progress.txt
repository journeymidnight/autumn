Date: 2026-04-01
TaskStatus: completed
Task scope: Group commit + remove local WAL + client-controlled must_sync (plan: curried-dazzling-canyon.md)

Current summary:
  Implemented all steps from the plan:

  1. Proto (already done in prior session):
     - PutRequest.must_sync, DeleteRequest.must_sync, StreamPutRequestHeader.must_sync

  2. Server — group-commit write loop + remove local WAL (already done in prior session):
     - WriteOp / WriteRequest types added to lib.rs
     - write_tx: mpsc::Sender<WriteRequest> in PartitionData (log_file/log_len removed)
     - background_write_loop: batches up to 128 requests, 1 append_batch RPC for entire batch
     - ALL entries (small + large) go to log_stream — no local WAL file
     - batch_must_sync = any(req.must_sync): if any request needs sync, whole batch syncs
     - put() / delete() handlers: thin — send to write_tx, await oneshot response
     - stream_put() passes header.must_sync through to put()
     - open_partition recovery: replays logStream from VP head; no local WAL step

  3. Client — --nosync flag (done this session):
     - WBench, Put, Del, StreamPut Command variants: added nosync: bool field
     - wbench parser: added --nosync flag
     - put/del parsers: added --nosync flag (flag-first, before positional args)
     - streamput parser: added --nosync flag
     - PutRequest/DeleteRequest/StreamPutRequestHeader: must_sync = !nosync
     - Usage string updated to show --nosync for wbench

  4. Docs updated:
     - crates/partition-server/CLAUDE.md: new write path, no local WAL, group commit
     - autumn-rs/CLAUDE.md: updated Core Write Data Flow diagram
     - autumn-rs/README.md: wbench --nosync example + description

  Files changed:
    crates/server/src/bin/autumn_client.rs
    crates/partition-server/CLAUDE.md
    autumn-rs/CLAUDE.md
    autumn-rs/README.md

  Tests: cargo test -p autumn-partition-server → 12/12 pass.
  Build: cargo build --workspace → clean.

Main gaps: none for this plan.

Next steps:
  F011 — mark umbrella as complete (all sub-features done)
  F035 — Extent node WAL for small-write durability
  F020 — gRPC connection pool with health check
