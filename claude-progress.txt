Date: 2026-04-02
TaskStatus: completed
Task scope: F041 — perf-check benchmark with regression warning

Current summary:
  F039 (binary search routing) and F041 (perf-check) both implemented in
  autumn-rs/crates/server/src/bin/autumn_client.rs.

  F039:
  - lookup_key(): O(log n) partition_point() binary search (Go sort.Search pattern)
  - refresh_regions(): contiguity validation with eprintln! warning on gaps

  F041 (perf-check subcommand):
  - Write phase: same loop as wbench, keys prefixed pc_
  - Read phase: same loop as rbench, uses written keys
  - PerfBaseline struct persisted to perf_baseline.json
  - Comparison: warn+exit(2) if ops/sec < baseline*threshold or p99 > baseline*(2-threshold)
  - --update-baseline flag saves current results as new baseline
  - --threshold (default 0.8), --duration (default 5s), --threads (default 4)

All tests pass:
  cargo test --workspace → all tests pass (0 failures)

Next steps:
  F035 (extent node WAL for small-write durability) — next P1 item not yet implemented.
