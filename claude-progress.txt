Date: 2026-04-18
TaskStatus: not_completed
Task scope: F095 — Perf R1 partition scale-out + batch cap sweep (Tier C, hypothesis refuted)

Current summary:
  Ran on branch `perf-r1-partition-scale-out` (worktree /data/dongmao_dev/autumn-perf-r1),
  base compio @ f09617e. Main `compio` worktree untouched. 15+ commits on the branch.

  Matrix (27 timed runs, 3 reps each except N=8 which failed bootstrap):
    A0 shm N=1         median w=51 236  r=82 315   p99w=20.66 ms
    A1 shm N=1         median w=52 637  r=73 462   p99w=20.02 ms   PS CPU 173 %
    A1 shm N=2         median w=45 017  r=82 359   p99w= 4.49 ms   PS CPU 522 %
    A1 shm N=4         median w=43 898  r=95 599   p99w= 3.52 ms   PS CPU 2000 %
    A1 shm N=8         BOOTSTRAP FAILED (manager RPC "connection closed" at 5/8 partitions)
    A4 3disk N=1       median w=50 331  r=75 738   p99w= 6.67 ms
    A4 3disk N=2       median w=33 141  r=100 886  p99w= 7.32 ms
    A5 multidisk-1node N=1 (replica=1) median w=52 305  r=81 872  p99w=7.63 ms

  T1 client-threads probe (shm N=1 cap=3072, 1 rep each):
    threads=256   w=49 873   p99=26 ms    r=70 394   p99=7 ms
    threads=512   w=55 855   p99=118 ms   r=63 211   p99=15 ms
    threads=1024  w= 6 090 💥 p99=814 ms   r=146 145  p99=9 ms

  Tier decision: TIER C (write peak 52.6 k < Tier B 80 k).
  Best cell: (N=1, cap=3072, storage=shm) — single partition + default batch cap.

  Attribution (dominant bottleneck = PS single P-log thread):
    replica fan-out (3→1)                <= 1 %  (A1 52.6 k ~= A5 52.3 k)
    disk (NVMe vs tmpfs)                 4 %     (A4 50.3 k ~= A1 52.6 k)
    multi-partition parallelism          NEG     (N=1 > N=2 > N=4 for writes)
    client concurrency                   peaks at 512 threads, collapses at 1024
    PS P-log thread saturation           DOMINANT (173 % CPU pegged; N=4 CPU 20 cores
                                                   but throughput drops to 44 k)
    ExtentNode                           NOT a bottleneck (CPU 14-36 % during writes)

Main gaps:
  - Tier C verdict: partition scale-out alone cannot reach 100 k.
  - Round 2 target: PS per-stream mutex lift + group-commit inner-loop profiling.
  - Manager N=8 bootstrap RPC robustness needs a fix before any future N>4 matrix.

Next steps:
  1. User decides whether to merge the Round 1 infrastructure (AUTUMN_GROUP_COMMIT_CAP env,
     cluster.sh --3disk/--multidisk-1node, perf_check.sh --partitions/--skip-cluster,
     multi-partition perf-check routing fix, bootstrap wait scaling, etcd pkill safety net)
     back to compio. All are strict improvements independent of the Tier verdict.
  2. If merged, perf_baseline_shm.json stays at F094 (52 k) since Round 1 did NOT improve
     single-partition throughput.
  3. Round 2 brainstorming opens with the attribution above as input. Skip ExtentNode
     multi-runtime, EC, client pool parallelism (all disproven as bottlenecks).

  Round 1 silver linings available if ever needed (no merge required to use them --
  just configure the cluster post-merge):
  - N>=2 -> write p99 drops 5.7x (20 ms -> 3.5 ms)
  - N=4  -> read ops/s +30 %
