Date: 2026-04-18
TaskStatus: not_completed
Task scope: F096 — Perf R2 profile-then-optimize (Tier C, Path iii attempted)

Current summary:
  Ran on branch `perf-r1-partition-scale-out` (worktree /data/dongmao_dev/autumn-perf-r1),
  base compio @ f09617e. Main `compio` branch untouched. 30+ commits on the R1+R2 branch.

  Phase 1 (diagnosis): 4 flamegraph SVGs via pprof-rs (perf binary missing; pprof is
  primary). Decision rules from spec §5.3 applied:
    C2 I/O wait           7.3 %        -- rule (i) does NOT fire
    C2 non-I/O hot fn     22.4 % (background_write_loop) -- rule (ii) fires
    C3 ps-conn aggregate  121.5 %      -- rule (iii) fires
    C3 mpsc contention    1.3 %        -- no
  Priority (i) > (iii) > (ii). (i) didn't fire -> **Path (iii) chosen**.

  Phase 2 (leader-follower WriteImpl analog):
    77cff64  WriteBatchBuilder skeleton + failing test
    e9ab495  test passes
    6df41fe  wire into handle_put + background_write_loop (feature-flagged)
             + AtomicWaker -> Vec<Waker> fix for 256-waiter broadcast
    f6d97ba  add collection window (await_first + 500us)
    cbb9b2d  tune default window 500us -> 100us (500us regressed to 36k)

  Final 3-rep medians (LF=on, 100us window, --shm N=1):
    write = 54 652 ops/s
    read  = 69 248 ops/s
    p99w  = 22.00 ms

  vs R1 N=1 median (52 637 / 73 462 / 20.02 ms):
    write +3.8 % (noise)
    read  -5.7 % (noise)
    p99w  comparable

  Tier verdict: C (miss 65k gate by ~10k).

  Root cause of Tier C:
  - 256 client threads * 4 ms ExtentNode RTT = ~64 k theoretical ceiling
  - Coalescing reduces per-request overhead but cannot break
    serialization x RTT product
  - Avg batch size under contention was 1.04 -- no effective grouping
  - Observed 54.6 k = 85 % of theoretical ceiling; implementation
    is near the fundamental limit

Main gaps:
  - Path (iii) at N=1 cannot close the gap via dispatch-side optimization alone
  - Round 3 needs architectural change: parallel P-log, quorum-on-2,
    client-side pipelining, or multi-PS partition isolation

Next steps:
  1. User decides whether to merge R1+R2 infrastructure back to compio
     (AUTUMN_LEADER_FOLLOWER env, pprof hook, perf_r2_* scripts,
      debug=1 + frame pointers, all revertible). Code changes on
      Path (iii) are default-off so merging is safe.
  2. If not merged: keep branch alive for Round 3.
  3. Open Round 3 brainstorming with this R2 data as input. Priority
     candidates for R3 evaluation: parallel P-log threads (revisit
     R2 Path i at higher client thread counts), client-side pipelining
     depth > 1, multi-PS partition isolation.

R2 net outputs (keepable on merge regardless of Tier verdict):
  - AUTUMN_LEADER_FOLLOWER env flag (default off = R1 path unchanged)
  - AUTUMN_LF_COLLECT_MICROS env knob (default 100us)
  - WriteBatchBuilder module as scaffold for future batching work
  - pprof-rs integration behind --features profiling
  - debug=1 + force-frame-pointers in release profile
  - perf_r2_thread_cpu.py (reusable diagnostic helper)
  - 4 flamegraph SVGs (reference data)
