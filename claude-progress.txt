Date: 2026-04-10
TaskStatus: completed
Task scope: Code cleanup — delete io-engine, remove tokio, split long lib.rs files

Current summary:
  Cleanup complete:
  1. Deleted autumn-io-engine crate (unused since compio migration)
     - Removed from workspace members, server Cargo.toml, bench references
     - Removed async-trait and parking_lot workspace deps (unused)
  2. Removed ALL tokio dependencies from autumn-rs crates
     - RpcClient: tokio::sync::{oneshot, Mutex} → futures::channel::oneshot + futures::lock::Mutex
       (Mutex needed because write_all().await crosses await point)
     - RpcServer: tokio::sync::mpsc → futures::channel::mpsc; writer Mutex same pattern
     - ConnPool: Arc → Rc, DashMap → HashMap (single-threaded compio)
     - partition-server: tokio::sync::{mpsc, oneshot} → futures::channel equivalents
     - Removed tokio from all Cargo.toml files including workspace
  3. Split long lib.rs files
     - manager/src/lib.rs: 2776 → 1189 lines (extracted rpc_handlers.rs 1154, recovery.rs 468)
     - partition-server/src/lib.rs: 2522 → 1324 lines (extracted background.rs 917, rpc_handlers.rs 319)
  All 49 unit tests pass. All 4 binaries build.

Key design insight (compio concurrency):
  - Rc<RefCell<T>>: use when borrow does NOT cross await points (fastest)
  - Rc<futures::lock::Mutex<T>>: use when lock MUST be held across await (e.g. write_all)
  - Never use std::sync::Mutex in async (blocks entire runtime)

Next steps:
  - Investigate write perf regression (5938 vs 17388 ops/s)
  - Update remaining CLAUDE.md files
