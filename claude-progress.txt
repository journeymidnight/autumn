Date: 2026-04-08
TaskStatus: not_completed
Task scope: F043 — Migrate ExtentService to autumn-rpc (data plane hot path)

Current summary:
  Major progress on F043. Core migration complete:
  - extent_rpc.rs: rkyv message types + binary codecs for all 10 RPCs (done)
  - conn_pool.rs: replaced gRPC ConnPool with autumn_rpc::ConnPool wrapper (done)
  - extent_node.rs: migrated to single-threaded compio server, all handlers converted (done)
    - Removed tonic/gRPC, tokio, autumn-io-engine, protobuf
    - Using compio::fs::File for disk I/O
    - Single-threaded TCP accept loop (no Dispatcher)
    - Rc/RefCell instead of Arc/Mutex
  - client.rs: migrated to autumn-rpc binary protocol (done)
    - Manager calls stubbed as TODO(F044)
    - futures::lock::Mutex for stream state
  - extent_node binary: #[compio::main] (done)
  - All 17 stream lib tests pass
  - autumn-stream crate compiles clean

Main gaps:
  1. Integration tests (crates/stream/tests/*.rs) need updating for new API
  2. stream_cli.rs needs migration to compio
  3. autumn-client.rs needs adaptation (F046 scope)
  4. partition-server won't compile (expected, F045 scope)
  5. WAL module still uses tokio internally — needs separate migration
  6. Remove tonic/tokio/autumn-proto/prost from Cargo.toml deps
  7. Update CLAUDE.md documentation

Next steps:
  Fix integration tests, update stream_cli, clean up Cargo.toml deps
