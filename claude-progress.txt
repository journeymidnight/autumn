Date: 2026-04-20
TaskStatus: completed
Task scope: Task #91 — F099-K N=4 scaling gap diagnosis (measurement + one recommended next step)

Branch: perf-r1-partition-scale-out
Worktree: /data/dongmao_dev/autumn-perf-r1
Main worktree /data/dongmao_dev/autumn (branch compio) MUST NOT be touched.

Current summary:
  Task #91 diagnosed the post-F099-K N=4 × d=8 scaling gap (57 k vs
  expected 4× of N=1's 63 k). Six hypotheses tested against probes
  (ss, top -H, bpftrace F099-H tcp_sendmsg/syscall_v2, ps.log
  stream append summary). Root cause: extent-node single-thread
  serialisation. See docs/superpowers/specs/2026-04-20-perf-f099k-
  n4-diagnosis.md.

Data captured (per hypothesis):
  H1 client-side imbalance: RULED OUT. Exactly 64 established TCP
     conns per partition port (9201..9204) over 6 samples, perfectly
     balanced — autumn-client stripes by tid % N (crates/server/src/
     bin/autumn_client.rs:1217).
  H2 extent-node shared bottleneck: CONFIRMED. avg_fanout_ms p50
     jumped from 9.6 ms (N=1 d=8) to 129.6 ms (N=4 d=8) — 13× slower
     per Phase-2 append. Extent-node thread %CPU stayed ≤ 1 core,
     but its single io_uring serialises 4 partitions' concurrent
     append streams. 9 total PS→extent TCP connections funnel into
     3 single-thread extent-node processes.
  H3 P-log per-partition: PARTIAL. All 4 part-N threads at 90-100 %
     CPU, evenly loaded. Per-partition throughput 14.85 k ops/s =
     4× LESS than N=1's 60.9 k/partition — same CPU, 4× less work
     per cycle because each P-log burns cycles waiting on the
     extent-node backlog.
  H4 client contention: RULED OUT. Client at 410-524 % total CPU
     (4-5 cores / 192) — nowhere near bound.
  H5 ConnPool: RULED OUT as independent cause. Per-partition
     RefCell<HashMap> means no cross-partition lock contention;
     9 PS→extent connections exist across 4 partitions' pools,
     each writer_task handled cleanly per F099-I-fix validation.
     H5 IS the mechanism of H2's queueing (RpcClient writers
     pipeline into an extent-node io_uring that serialises them).
  H6 bootstrap skew: RULED OUT by design. thread_targets[tid] =
     partitions[tid % N] distributes BY THREAD INDEX not BY HASH.

Cross-validation:
  Depth sweep N=4: d=1 = 42.7 k, d=8 = 59.4 k, d=16 = 64.8 k (peak)
  Depth curve identical to N=1 — 4× more partitions gives ZERO
  collective throughput gain. Strong evidence of shared bottleneck.

Kernel TCP (PS 30-s window):
  N=1 d=1 F099-H ceiling: 2.78 cores (tcp_sendmsg 24 s + recvmsg 59 s)
  N=1 d=8:  0.74 cores (3.8× less than ceiling — F099-I coalescing win)
  N=4 d=8:  0.24 cores (11× less than ceiling — coalescing + partition
                         fan-out spreads kernel work across 4 listeners)
  N=4 d=16: 2.21 cores (still 1.3× under ceiling — not the binding)

Recommendation — ONE concrete next step:
  Multi-thread the ExtentNode append path. Give autumn-extent-node
  K compio runtimes (one per OS thread, default K=num_cpus.min(8))
  keyed off accepted-fd hash. The single TcpListener on 9101 stays;
  accept forwards fds to worker runtimes. extents: Rc<DashMap> →
  Arc<DashMap> (DashMap is already thread-safe lock-free per shard).
  Per-extent append ordering preserved by Arc<Mutex<ExtentEntry>>
  inside the map. File-change pattern mirrors F099-K's partition-
  server refactor (open_partition → per-thread fd_tx/fd_rx).

  Expected throughput gain: fanout p50 drops 13× → N=4 d=8 should
  rise 1.8–2.5× to 110–150 k ops/s (ideal ceiling 240 k if fan-out
  were the only binding). Write p99 drops proportionally.

  Files: crates/stream/src/extent_node.rs (primary), crates/server/
  src/bin/extent_node.rs (--worker-threads flag + plumbing).

  Effort: 3–4 days. Risk: medium (DashMap already concurrent-safe;
  invariants are preserved; owner_lock revision still CAS'd per
  extent). Low risk of perf regression at N=1 (accept-then-route
  overhead is a few ns per fd, one-time per connection).

Perf numbers (3-rep medians where available; 256 × 4 KB × nosync × tmpfs):
  N=1 × d=1:  write 41.9 k / read 48.1 k  (F099-I-fix, baseline)
  N=1 × d=8:  write 60.9–68.3 k / read 38.5–41.2 k (run-to-run variance)
  N=4 × d=1:  write 42.7 k / read 48.9 k (≈ N=1 × d=1)
  N=4 × d=8:  write 41.3–59.4 k / read 44.0–45.8 k (LARGE variance)
  N=4 × d=16: write 45.1–64.8 k / read 55.3–73.8 k (peak vs sustained)

Commits this session:
  (pending) docs(F099-K-diagnosis): N=4 scaling gap root-caused —
           extent-node single-thread serialisation

Main gaps (carry into next session):
  1. Implement the multi-thread ExtentNode recommendation above.
  2. F099-J standalone `passes: false` still on record (cosmetic;
     F099-I + F099-I-fix already restored N=1 parity).

Next steps (priority order):
  STEP 1 (next session): implement multi-thread ExtentNode (recommendation
    from this diagnosis). 3-4 days. Gate: N=4 × d=8 write ≥ 110 k,
    N=4 × d=8 fanout p50 < 20 ms, N=1 × d=1 write ≥ 41 k (no regression).
  STEP 2: retire the "57 k ceiling" narrative; next ceiling analysis
    against the multi-thread ExtentNode numbers.
  STEP 3: feature_list F100 entry for the ExtentNode multi-thread feature.

Do NOT touch main worktree at /data/dongmao_dev/autumn (branch compio).

The work has been commit-clean.
