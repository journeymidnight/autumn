Date: 2026-04-08
TaskStatus: not_completed
Task scope: F043 — Migrate ExtentService to autumn-rpc (data plane hot path)

Current summary:
  F043 nearly complete. All core migration done + integration tests passing:
  - extent_rpc.rs: rkyv message types + binary codecs for all 10 RPCs (done)
  - conn_pool.rs: single-threaded compio ConnPool (Rc/RefCell, no Arc) (done)
  - extent_node.rs: compio server, all handlers converted (done)
  - client.rs: autumn-rpc binary protocol, Rc<ConnPool> (done)
  - wal.rs: fully rewritten — sync blocking I/O, batch write support, no tokio (done)
  - extent_node binary: #[compio::main] (done)
  - stream_cli.rs: alloc-extent/commit-length via autumn-rpc, manager cmds stubbed (F044) (done)
  - Integration tests: all 11 pass (compio::test, TestConn helper) (done)
  - Unit tests: 18 pass (done)
  - Removed tonic/prost/tokio/autumn-proto/autumn-io-engine from stream Cargo.toml
  - rkyv_decode: aligned buffer handling for wire data

Main gaps:
  1. partition-server won't compile (expected, F045 scope) — uses Arc<ConnPool>
  2. autumn-server crate can't build all binaries until F045
  3. stream CLAUDE.md updated, but root CLAUDE.md may need updates
  4. Feature list not yet marked passes=true (pending final review)

Next steps:
  - Update CLAUDE.md and feature_list.md
  - Commit all changes
  - Move to F044 (manager migration) or F045 (partition-server migration)
