Date: 2026-03-27
TaskStatus: completed
Task scope: F034 – Extent node metadata persistence

Current summary:
  Implemented per-extent sidecar file `extent-{id}.meta` (40 bytes, binary LE):
  magic(8) + extent_id(8) + sealed_length(8) + eversion(8) + last_revision(8).
  Written on: alloc_extent, seal event (via apply_extent_meta), run_recovery_task,
  re_avali, revision change in append/commit_length.
  Zero overhead on normal append path.
  block_sizes intentionally not persisted — partition layer owns block-level semantics.
  load_extents() scans data dir for extent-*.dat on startup, restores all extents.
  3 integration tests pass: restart_preserves_commit_length, restart_preserves_meta_fields,
  restart_extent_remains_writable.
  Existing tests unchanged (5 total pass).

Main gaps:
  None for F034.

Next steps:
  F030 – Three-stream model with metaStream persistence (partition layer)
  F029 – Compaction engine with merge iterator
