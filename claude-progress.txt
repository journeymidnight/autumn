Date: 2026-04-09
TaskStatus: not_completed
Task scope: F044 — Migrate Manager services to autumn-rpc

Current summary:
  F044 core migration complete. AutumnManager rewritten from tonic/tokio/etcd-client
  to compio + autumn-rpc + autumn-etcd. All 16 RPC handlers implemented via dispatch().
  EtcdMirror migrated to autumn-etcd. Background loops migrated to compio::runtime::spawn.
  MetadataStore migrated from Arc<RwLock> to Rc<RefCell>.

  StreamClient 12 TODO stubs implemented — all manager calls via ConnPool + rkyv.
  ExtentNode 3 stub methods implemented (extent_info_from_manager, nodes_map_from_manager,
  resolve_recovery_extent).

  Manager binary (autumn-manager-server) switched to #[compio::main].

  New crate/module: crates/rpc/src/manager_rpc.rs — 16 MSG_TYPE constants, rkyv message
  types for all manager RPCs, plus extent node RPC types needed by manager.

  5 manager unit tests pass. 18 stream unit tests pass.

  Blocked: other binaries in autumn-server crate (autumn-client, autumn-ps, stream-cli)
  still use tonic gRPC clients for manager services — they won't compile until F045/F046.

Main gaps:
  - F045: PartitionKv service migration (partition-server crate uses tonic)
  - F046: CLI tools + proto codegen + integration tests migration
  - autumn-server crate doesn't fully compile because autumn-client/ps still use tonic
  - Integration tests not yet migrated to compio runtime

Next steps:
  - Commit F044 changes
  - Start F045 (Migrate PartitionKv service to autumn-rpc)
