Date: 2026-04-02
TaskStatus: completed
Task scope: Performance optimization — remove StreamClient Mutex, increase flush threshold, increase batch size

Current summary:
  All three performance changes implemented and tested:
  1. StreamClient lock-free (no external Mutex):
     - Replaced Arc<Mutex<StreamClient>> → Arc<StreamClient> everywhere
     - Per-stream locking inside StreamClient via DashMap<stream_id, Arc<Mutex<StreamAppendState>>>
     - All StreamClient methods take &self (no &mut self)
     - Parallel fan-out to replicas (tokio::spawn per replica)
  2. FLUSH_MEM_BYTES: 256KB → 64MB (matches Go MaxSkipList)
     - Removed FLUSH_MEM_OPS = 512 condition from maybe_rotate_locked
  3. MAX_WRITE_BATCH: 128 → 256

Additional fixes:
  - Fixed sealed-extent bug in StreamClient: after split, tail extent is sealed.
    Compact's row_stream append now detects sealed tail and allocates new extent.
  - Integration tests updated: replaced 300KB-filler flush trigger with explicit
    flush_partition() method; added flush_partition() public API to PartitionServer.
  - Pre-existing PartitionServer::connect signature mismatch in integration.rs fixed.

All tests pass:
  cargo test --workspace → all 38+ tests pass

Next steps:
  Deploy and benchmark wbench to verify performance improvement vs Go.
