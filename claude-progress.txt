Date: 2026-03-28
TaskStatus: completed
Task scope: F038 – Remove block_sizes from stream layer (simplify to pure byte store)

Current summary:
  Removed block_sizes from stream layer; stream is now a pure byte store.
  - ExtentEntry.block_sizes (Mutex<Vec<u32>>) removed entirely.
  - normalize_block_sizes() deleted.
  - truncate_to_commit() simplified to byte-granular file.truncate(commit).
  - Proto: AppendRequestHeader.blocks removed (reserved 6); AppendResponse.offsets → offset (single u32).
  - Proto: ReadBlocksRequest/Response renamed to ReadBytesRequest/Response; num_of_blocks→length; block_sizes/offsets removed.
  - Proto: Location.len added (u32) for SST byte-range reads.
  - StreamClient: append_payload/batch returns single AppendResult; AppendBatchResult removed.
  - StreamClient: read_blocks_from_extent → read_bytes_from_extent(extent_id, offset, length).
  - StreamClient: read_last_block → read_last_extent_data (reads entire last non-empty extent).
  - Partition server: TableMeta.len field; save_table_locs_raw uses length-delimited encoding.
  - Partition server: metaStream read uses decode_last_table_locations() walker for length-delimited entries.
  - Partition server: logStream replay replaced block iteration with full extent read + decode_record_metas.
  - Partition server: read_value_from_log accepts record_len computed from KeyMeta at call site.
  - All tests pass (stream, manager integration, partition-server).

Main gaps:
  None for F038.

Next steps:
  F032 – SSTable bloom filter, prefix compression, block cache
  F033 – GC with discard tracking (depends on F029 discard maps, F031 value log)
  F037 – Partition split with overlap detection and major compaction
