Date: 2026-04-10
TaskStatus: completed
Task scope: F045 — Migrate PartitionKv service to autumn-rpc (Thread-per-Partition)

Current summary:
  F045 complete. PartitionServer rewritten with thread-per-partition architecture:
  - Main thread (compio): accept connections, route by part_id, heartbeat/region sync
  - Per-partition threads: each with own compio Runtime, Rc/RefCell (no locks)
  - Inter-thread communication: tokio::sync::mpsc (runtime-agnostic) for request routing

  8 PartitionKv RPCs (Put, Get, Delete, Head, Range, SplitPart, StreamPut, Maintenance)
  migrated from tonic gRPC to autumn-rpc with rkyv serialization (msg types 0x40-0x47).

  Manager client calls (register_ps, heartbeat_ps, get_regions) use autumn-rpc ConnPool.

  Background loops (write, flush, compact, gc) all on partition thread via compio::spawn.
  tokio::select! replaced with manual poll_fn for compact/gc loops.

  Arc<RwLock<PartitionData>> → Rc<RefCell<PartitionData>> (single-threaded per partition).
  tonic, async-trait, parking_lot, dashmap removed from partition-server deps.

  New: crates/rpc/src/partition_rpc.rs — 8 MSG_TYPE constants, rkyv request/response types.

  34 unit tests pass (5 manager + 18 stream + 11 partition-server).
  Partition server binary switched to #[compio::main].

Main gaps:
  - F046: CLI tools (autumn-client, stream-cli) + proto codegen + integration tests
  - autumn-server crate still won't fully compile (CLI binaries use tonic)

Next steps:
  - Commit F045
  - Start F046 (Migrate CLI tools, proto codegen, and tests to compio)
