Date: 2026-04-13
TaskStatus: not_completed
Task scope: PS Dispatcher — spread connections across worker threads

Current summary:
  Refactored PartitionServer to use compio Dispatcher pattern (same as RpcServer):
  - Dedicated accept OS thread (blocking std::net::accept)
  - N worker threads via compio::Dispatcher for connection handling
  - PartitionRouter (DashMap<part_id, mpsc::Sender>) shared with workers
  - Control plane (heartbeat, region_sync) stays on main compio thread
  - Sequential per-connection processing preserved (no Mutex writer)
  - Configurable via --conn-threads N (default: CPU count)

  Build: OK, all 12 unit tests pass.

  NOT YET TESTED: need to start cluster and run perf-check to verify
  performance improvement and correctness under load.

Main gaps:
  - End-to-end integration test not done yet
  - Need perf-check comparison before vs after

Next steps:
  - Run full cluster (manager + extent-node + PS) with perf-check
  - Compare write/read ops/s before vs after
  - Verify CPU usage spreads across cores (htop)
  - Commit if results are positive
