Date: 2026-04-02
TaskStatus: completed
Task scope: F039 — client-side partition routing (binary search + contiguity validation)

Current summary:
  Implemented F039 interim solution in autumn-rs/crates/server/src/bin/autumn_client.rs:
  - lookup_key(): replaced O(n) linear scan with O(log n) partition_point() binary search
    matching Go's sort.Search pattern (find first region with end_key > key)
  - refresh_regions(): added contiguity validation loop after sort — warns on gaps
    between adjacent region end_key/start_key (Go saveRegion lines 61-65)
  - Cache-then-refresh-once pattern retained (no etcd watch — deferred)
  - Thread safety skipped — ClusterClient is CLI-only, no concurrent access

  Also committed separately: HTTP/2 tuning (adaptive window, keepalive),
  fd limit bump via libc, release profile (LTO+codegen-units=1+panic=abort),
  removed channels_per_ps (each wbench thread gets its own connection).

All tests pass:
  cargo test --workspace → all tests pass (0 failures)

Next steps:
  F035 (extent node WAL for small-write durability) — next P1 item not yet implemented.
