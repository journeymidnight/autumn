Date: 2026-04-01
TaskStatus: completed
Task scope: F037 – Partition split with overlap detection and major compaction

Current summary:
  Implemented F037: overlap detection on partition open, split rejection when overlap exists,
  out-of-range key filtering in compaction, and range-scan filtering when overlap is present.

  Changes in autumn-rs/crates/partition-server/src/lib.rs:
  - open_partition: after loading each SstReader, check smallest_key/biggest_key against
    partition range via parse_key() + in_range(). Set detected_overlap=true if any key is
    out of range. Initialize has_overlap AtomicU32 with 1 if detected_overlap.
  - split_part: check has_overlap before expensive unique_user_keys_async; return
    FAILED_PRECONDITION if overlap exists ("cannot split: partition has overlapping keys,
    run major compaction first").
  - do_compact: capture partition range (rg) alongside readers; after dedup but before
    major-mode tombstone filter, skip keys outside partition range (add to discard map).
    Applies to both major and minor compaction. Changed minimum table count from 2 to 0
    (empty guard only); background_compact_loop skips major compaction if < 2 tables AND
    no overlap (allows single-table major compaction to clean up overlapping keys).
  - range(): load check_overlap + part_rg before the merge loop; skip keys outside
    partition range when check_overlap is true.
  - Added has_overlap(part_id) public helper for testing.

  Tests:
  - Integration test: f037_overlap_detected_after_split_and_cleared_by_compaction
    1. Creates partition [a, z), writes keys in "a*" and "y*" ranges (with large fillers
       to trigger flushes → SSTables cover full [a, z) range).
    2. Splits partition → creates two children sharing the parent's SSTables.
    3. Verifies both children detect overlap (has_overlap=true).
    4. Verifies split_part on overlapping partition returns FAILED_PRECONDITION.
    5. Verifies range scan on left child returns only in-range keys.
    6. Triggers major compaction on left child.
    7. Verifies has_overlap cleared and all in-range keys still readable.
  - All 12 unit tests + 13 integration tests pass.

Main gaps:
  None for F037.

Next steps:
  F035 – Extent node WAL for small-write durability
  F010 – Partition API parity with Go legacy endpoints
  F020 – gRPC connection pool with health check
