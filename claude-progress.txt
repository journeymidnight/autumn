Date: 2026-04-15
TaskStatus: completed
Task scope: F082 — ClusterClient auto-reconnect and multi-manager

Current summary:
  F082 (completed): ClusterClient auto-reconnect + multi-manager support.
    - Manager connections: auto-reconnect on error + round-robin failover
    - PS connections: auto-reconnect on error (drop + reconnect on next call)
    - connect() accepts comma-separated manager addresses
    - CLI autumn_client.rs updated for new mgr() -> Result API

  Previously completed this session:
    F077: Etcd-first mutation pattern
    F078: Per-disk health check
    F079: Multi-manager for StreamClient + PartitionServer
    F062-F076: 16 system tests
    F082: ClusterClient auto-reconnect

Next steps:
  - Multi-manager system test with embedded etcd
