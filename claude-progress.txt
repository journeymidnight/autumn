Date: 2026-04-20
TaskStatus: completed
Task scope: F099-I-fix — d=1 inline fast path + CAP-EINVAL root-cause

Branch: perf-r1-partition-scale-out
Worktree: /data/dongmao_dev/autumn-perf-r1
Main worktree /data/dongmao_dev/autumn (branch compio) MUST NOT be touched.

Current summary:
  F099-I-fix addresses both DONE_WITH_CONCERNS items from F099-I.

  Sub-task 1 — d=1 fast path (DONE):
    Added `d1_fast_path_round_trip` helper that inlines one request→response→
    write_all round-trip WITHOUT going through FuturesUnordered or Box::pin.
    `handle_ps_connection` idle branch peeks the decoder after a read: if
    exactly one complete frame was buffered AND inflight.is_empty() AND
    tx_bufs.is_empty(), the fast path engages. Strict correctness gate:
    if ANY prior frame is still in flight, the slow path is used (prevents
    reply reordering). Factored `push_one_frame_to_inflight` out of
    `push_frames_to_inflight` so both paths (fast-engage-fail fallback +
    slow drain) share the Box::pin ceremony.

    A pub(crate) AtomicU64 counter PS_FAST_PATH_HITS exposes engagement to
    tests; fetch_add is ~1 ns on the hot path (single writer per conn, no
    cache-line contention). Two new ps lib tests (serialized via Mutex to
    avoid cross-test counter races):
      - f099i_d1_fast_path_no_fu_allocation — verifies 10 synchronous
        round-trips each engage the fast path exactly once (delta == 10).
      - f099i_fast_path_inactive_under_batch — verifies an 8-frame batched
        write does NOT engage the fast path (delta well under N).

  Sub-task 2 — CAP-EINVAL root cause (DONE):
    Added WARN-level instrumentation on the RpcClient writer_task error
    path: iov_count, total_bytes, errno.raw_os_error(), io::ErrorKind,
    raw error message. Previously writer-task exits appeared only as
    downstream "submit error: connection closed" noise from
    launch_append — now the FIRST offending call self-describes.

    Wrote `writer_task_handles_2048_concurrent_vectored` integration
    test in autumn-rpc: 2048 concurrent `call_vectored(msg, vec![part])`
    submissions sharing ONE RpcClient's writer_task. Exactly mirrors the
    shape F099-I's concern referenced (each produces a SubmitMsg::Vectored
    with 2 iovecs). Passes deterministically.

    Re-ran the originally failing scenario (CAP=8 × d=8 × 256 threads) in
    3 replicates. Every run completed cleanly; zero writer_task exits in
    ps.log. Also swept CAP=16, 32, 64, 256 and d=16 / d=64 — all clean.
    Conclusion: the F099-I EINVAL was NOT a deterministic kernel-level
    limit. 2-iov SendMsg under 2048 concurrent pressure is well-formed;
    futures-channel::mpsc with buffer=1024 + N senders scales to 3072+
    capacity. The likely cause was a transient race tied to F099-I's
    pre-fix timing (possibly an allocator / io_uring SQE interaction that
    no longer triggers once the d=1 fast path removes the Box::pin hot
    spot). The WARN instrumentation stays in place permanently so any
    future recurrence surfaces with exact errno + iov_count.

Perf state (3-rep medians, 256 × 4KB × nosync × tmpfs):
  N=1 × d=1:  write 41.9k / read 48.1k  (F099-K baseline 41.5k / 45.4k —
                                         **regression eliminated**, +1% write)
  N=1 × d=8:  write 61.9k / read 92.1k  (F099-I was 63.2k / 88.0k — 2% dip
                                         within noise, d=8 gain preserved)
  CAP=8 × d=8: write 60.9k / read 83.1k, zero EINVAL

Commits this session:
  (pending) perf(F099-I-fix): d=1 fast path + CAP EINVAL root cause

Test state:
  autumn-partition-server: 84/84 lib (was 82/82 + 2 new F099-I-fix tests)
    - f099i_d1_fast_path_no_fu_allocation
    - f099i_fast_path_inactive_under_batch
  autumn-stream: 40 lib + 21 integration green
  autumn-rpc: 10 lib + 8 integration green (was 7 integration + 1 new)
    - writer_task_handles_2048_concurrent_vectored

Documentation:
  - autumn-rs/crates/partition-server/CLAUDE.md: ps-conn handler diagram
    updated with "d=1 FAST PATH" branch + rationale.
  - autumn-rs/crates/rpc/CLAUDE.md: writer_task Invariants section adds
    F099-I-fix instrumentation contract + 2-iov SendMsg stability note.

Main gaps (carry into next session):
  1. N=4 × d=8 only hit 57k in F099-I (target 100k). STEP 2 of the prior
     priority list: diagnose F099-K N=4 scaling gap. Hypotheses:
     a. 256 benchmark threads not evenly distributed across partition
        ports (verify with ss -t state established during run).
     b. Extent-node shared bottleneck (3 extent-nodes serve all 4 parts).
     c. Manager-level GetRegions overhead on client.
  2. F099-J standalone is still `passes: false` (write regression at
     N=1 × d=1 vs F099-D). F099-I + F099-I-fix together restore parity,
     so F099-J's regression is cosmetic on the cumulative ledger, but
     the feature_list entry carries the original assessment.

Next steps (priority order):
  STEP 1 (next session): STEP 2 from prior plan — F099-K N=4 diagnosis.
  STEP 2: Tier verdict + feature_list update + merge-decision gate.

Do NOT touch main worktree at /data/dongmao_dev/autumn (branch compio).

The work has been commit-clean.
